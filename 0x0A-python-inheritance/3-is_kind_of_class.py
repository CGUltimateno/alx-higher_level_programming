#!/usr/bin/python3
"""Defines a class that returns True if the object is an instance"""


def is_kind_of_class(obj, a_class):
    """Returns True if the object is an instance"""
    return isinstance(obj, a_class)
